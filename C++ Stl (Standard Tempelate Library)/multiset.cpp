// Inbuilt implementation -> Red Black tress
// Time complexity - O(log n)
// Duplicate values and elements are stored in sorted order

#include<bits/stdc++.h>
using namespace std;
int main()
{
    multiset<int> m;

    // Inserting values
    m.insert(1);
    m.insert(1);

    // Output
    for(auto &item : s)
    {
        cout << item << endl;
    }
    return 0;
}
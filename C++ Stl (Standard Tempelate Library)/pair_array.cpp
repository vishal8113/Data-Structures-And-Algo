#include<bits/stdc++.h>
using namespace std;
int main()
{
    int N;
    cin >> N;
    pair<int,int> p_array[N]; // declaration of array of pairs of N size

    // Taking input
    for(int i = 0; i < N; ++i)
    {
        int firstNum;
        int secondNum;
        pair = {firstNum,secondNum};
    }

    // Printing Output
    for(int i = 0; i < N; ++i)
    {
        cout << p_array[i].first << " " << p_array[i].second << endl;
    }
    
    return 0;
}